.qsc-registry-login:
  image: reg.qschou.com/ops/docker:27.4.0-dind
  services:
    - reg.qschou.com/ops/docker:27.4.0-dind 
  script: 
    - echo $CI_REGISTRY_PASSWORD | docker login -u $CI_REGISTRY_USER $CI_REGISTRY --password-stdin
